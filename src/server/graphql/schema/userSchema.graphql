export const authTypeDefs = `#graphql
  type User {
    id: Int!
    email: String!
  }
  type authAPIResponse {
    user: User!
    token: String!
  }
  type Query {
    getUserByID(id: Int!): User
    getAllUsers: [User!]!
  }
  type Mutation {
    signup(email: String!, password: String!): authAPIResponse!
    login(email: String!, password: String!): authAPIResponse!
  }
`


